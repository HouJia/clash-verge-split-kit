# 分流验证 Playbook（可重复步骤）

## 前言

本文是 **[自用分流说明（Verge 扩展）](local-split-vps.md)** 的配套：**改规则或切换出站之后**，按固定顺序在 Clash Verge 内核对「流量实际走了哪条路径」，并用 **`hjs-egress-ip` CLI** 与可选的 **`hjs-egress-ip serve` 审计 UI** 做出口侧对照。  
主叙事（Extend、`*-rule-split-extend.yaml`、成对的 `*-rule-split.local.yaml`、免责声明）仍以 `local-split-vps.md` 为准；此处只收敛**验证动作**。

**验证目的：** 在保存/重载配置后，确认目标站点或进程命中 **DIRECT** 或你期望的**代理组**（组名须与合并后的运行时配置一致）。

---

## 前置

- Clash Verge 使用 **规则（Rule）模式**；分流才按 `rules` 生效（与 `local-split-vps.md` 一致）。
- 已在 Verge 中加载 **全局扩展**，且扩展内容与所选 **`VERGE_EXTEND_FILE`**（或默认机场稿）成对的 **`verge/generated/*-rule-split.local.yaml`**（均由 **`bash verge/scripts/render-local.sh`** 写出）一致；勿提交上述本机稿或 **`verge/generated/local/override.local`**。**密钥类内容勿写入公开文档。**
- 客户端菜单、「当前运行时配置」「Connections」等名称以 **你本机已安装的 Clash Verge / mihomo** 为准。

---

## 通用步骤骨架（VER-01）

改规则或切节点后，建议按下面顺序重复执行（编号即推荐顺序）：

1. **保存并重载**  
   在 Verge 中保存配置并触发重载（或切换节点后等待配置生效）。
2. **核对「当前运行时配置」（只读）**  
   打开只读的合并后配置视图，确认 **`mode` 为 `rule`**（或界面等价表述为规则模式），并抽查与本次改动相关的 **`rules` 片段**是否与预期一致。
3. **在 Connections（连接）或本机等价的「连接」视图中观察**  
   对目标域名或发起进程，查看其 **策略链 / 命中策略组** 是否指向 DIRECT 或你期望的代理组。
4. **（可选）日志**  
   若客户端提供规则命中或内核日志入口，可辅助确认某条规则是否被命中；**不能**替代第 2、3 步在 Verge 内的观察。

**系统侧可选核对：** 可用 `curl -4` 等从 shell 探测出口，**仅作补充**；**不能**替代 Verge 内对 `mode`、`rules` 与 Connections 的核对。

---

## 场景示例（至少两类）

以下组名请 **替换为你订阅内真实存在的策略组名**（与 Phase 2 文档表述一致）；下文用「**主代理组**」指代该组。

### （a）期望走 DIRECT 的国内常见站

- **触发：** 浏览器访问国内常见 HTTPS 站点（示例域名可用 `www.example.cn` 类占位理解；实际测试请用你日常访问的站点）。
- **在 Connections 中看什么：** 该连接的策略链末端为 **DIRECT**（或等价直连表述），与扩展模板中 **国内/直连类 GEOSITE/GEOIP 规则** 的叙事一致。
- **与仓库分流叙事对应：** 命中「国内地址段 / 国内域名集合 → DIRECT」类规则，而非落入默认的 **主代理组**。

### （b）期望走主代理组的境外或默认走代理的流量

- **触发：** 浏览器访问需经代理访问的站点（勿在文档中粘贴真实订阅或面板 URL）。
- **在 Connections 中看什么：** 连接走 **主代理组**（你替换后的组名），并在该组下落到具体节点。
- **与仓库分流叙事对应：** 未命中直连规则时，由 `MATCH` 或默认规则落入 **主代理组**，与仓库中 `rules` 设计意图一致。

---

## 何时运行 `hjs-egress-ip`（VER-02）

在 **Verge 重载、切换节点或修改规则** 之后，当你需要把「Verge 内看到的路径」与「对外出口 IP」做对照时运行 CLI：

- **无参摘要：** 终端执行 `hjs-egress-ip`，查看分组的一键出口摘要（与 `hjs-egress-ip-cli/README.md` 一致）。
- **机器可读表头：** `hjs-egress-ip --simple-tsv | head -5`，首行表头须含字面串 **`scenario`**（与 README 一致）。
- **如何与预期对照：** 将输出中的运营商/大致地域与「我此时期望走代理机房 / 本地直连」的心理模型对照；**不**承诺具体公网 IP 长期不变。

---

## 审计 UI：`hjs-egress-ip serve`

与 CLI 同属一条工具链，提供浏览器入口（详见 `hjs-egress-ip-cli/README.md`）：

1. 在终端执行 **`hjs-egress-ip serve`**，按提示在浏览器打开给出的 URL。
2. 页面上使用 **「一键检测」** 触发与 CLI 摘要互补的交互结果。
3. 健康检查：在另一终端执行  
   `curl -s http://127.0.0.1:18765/health`  
   期望输出为 **`ok`**（与 `hjs-egress-ip-cli/README.md` 第 31 行一致）。

**与 CLI 的关系：** 同一套探测逻辑，**CLI** 适合脚本与流水线；**审计 UI** 适合本机快速点点对照；二者都与上文「改规则/切节点后复查」衔接。

---

## 检查清单（VER-01 + VER-02）

下列表格将 **Verge 内步骤** 与 **CLI / 审计 UI** 显式对应；「通过判据」为最小可接受结果。

| 步骤 | Verge 核对 | CLI / 审计 UI | 通过判据 |
|------|------------|----------------|----------|
| 1. 保存并重载 | 确认已重载或节点切换完成 | （本步可不跑 CLI） | 无报错；配置已应用 |
| 2. 运行时配置 | **Rule 模式**；相关 `rules` 片段可见且符合预期 | （可选）改后再跑 `hjs-egress-ip` 对照出口 | `mode` 为规则模式；规则叙事与场景一致 |
| 3. Connections | 目标连接策略链为 DIRECT 或 **主代理组** | `hjs-egress-ip` 无参摘要与「直连/代理」预期大致一致 | 策略链与出口叙事不矛盾 |
| 4. 机器可读抽查 | （与 3 同步理解） | `hjs-egress-ip --simple-tsv \| head -5` 表头含 **`scenario`** | 表头含 `scenario` 字面值 |
| 5. 审计 UI（可选） | 浏览器与 Verge 同机操作即可 | 启动 `hjs-egress-ip serve`；浏览器 **一键检测**；`curl -s http://127.0.0.1:18765/health` | 健康接口返回 **`ok`** |

---

## 术语

与 **`local-split-vps.md`** 一致：**Extend / 扩展配置**、**全局扩展**、模板中的 **`PROXY`** 须替换为订阅内真实主组名；**「当前运行时配置（只读）」**、**Connections（连接）** 等以本机客户端为准。
