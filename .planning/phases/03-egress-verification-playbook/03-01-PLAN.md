---
phase: 03-egress-verification-playbook
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/clash-verge/verification-playbook.md
  - docs/clash-verge/README.md
  - docs/clash-verge/local-split-vps.md
autonomous: true
requirements:
  - VER-01
  - VER-02
must_haves:
  - "可重复步骤：改规则或切节点后能按固定顺序复查 DIRECT / 指定策略组"
  - "各关键步骤写明 Verge 内可观察指标（Rule 模式、当前运行时配置、Connections/连接视图、必要时日志）"
  - "写明何时运行 hjs-egress-ip（及可选 --simple-tsv）、如何解读输出与分流预期对照"
  - "hjs-egress-ip serve 审计 UI：启动、浏览器一键检测、curl /health 期望 ok（与 hjs-egress-ip-cli/README.md 一致）"
  - "条目化检查清单，并与审计 UI 打开—加载—对照流程显式对应（表格或章节）"
  - "docs/clash-verge/README.md 链入 verification-playbook.md"
  - "公开文档无真实订阅 URL（rg 口径与 Phase 2 计划一致）"
---

# Plan 03-01：分流验证 Playbook 与工具链对齐（VER-01、VER-02）

<objective>
在 `docs/clash-verge/` 交付**可重复验证说明**，满足 VER-01（步骤级 + Verge 可观察指标）与 VER-02（与 `hjs-egress-ip` CLI 及 `serve` 审计 UI 的检查清单式衔接）；并更新入口与 `local-split-vps.md` 交叉引用。
</objective>

<threat_model>
**ASVS L1（文档）：** 防止在公开树写入真实订阅 URL、令牌与节点密钥（信息泄露）。**缓解：** 全文占位符；示例主机名用 `example.com` 类；不粘贴 `https://.../clash/...` 真实面板链接。**残余：** 作者粘贴失误 — 由 `acceptance_criteria` 中 rg 与 PR 自检兜底。
</threat_model>

<tasks>

<task type="execute">
  <name>新建 verification-playbook.md（主体）</name>
  <files>docs/clash-verge/verification-playbook.md</files>
  <read_first>docs/clash-verge/local-split-vps.md</read_first>
  <read_first>hjs-egress-ip-cli/README.md</read_first>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>
    新建 `docs/clash-verge/verification-playbook.md`（中文），结构至少包含：
    1. **前言：** 与 `local-split-vps.md` 的关系；验证目的（改规则/节点后确认流量路径）。
    2. **前置：** Clash Verge **规则（Rule）模式**；已加载 `verge/extend/global-split.yaml` 与订阅；客户端版本以本机为准（与现有免责声明一致）。
    3. **通用步骤骨架（VER-01）：** 编号列表：保存/重载配置 → 在 **当前运行时配置（只读）** 核对 `mode` 与关键 `rules` 片段 → 在 **Connections（连接）**（或你本机等价视图）观察目标域名/进程的 **策略链** → 可选：**日志** 中规则命中线索。
    4. **场景示例（至少 2 类）：** （a）期望 **DIRECT** 的国内常见站；（b）期望走 **主代理组**（名称须写「替换为你订阅内真实组名」，与 Phase 2 表述一致）。每场景给出：触发方式（浏览器访问何类 URL）、在 Connections 中看什么、与 `global-split.yaml` 中 GEOSITE/GEOIP 叙事如何对应。
    5. **系统侧可选核对：** 一句说明可用 `curl -4` 等辅助，**不能**替代 Verge 内观察。
    6. **hjs-egress-ip（VER-02）：** 小节「何时跑 CLI」：在 Verge 重载/切节点/改规则后；命令：`hjs-egress-ip` 无参看分组摘要；`hjs-egress-ip --simple-tsv | head -5` 表头须含字面串 `scenario`（与 README 一致）。说明如何与「预期出口」对照（同运营商/大致地域 vs 代理机房），**不**承诺具体 IP。
    7. **审计 UI：** 小节「hjs-egress-ip serve」：终端启动；浏览器打开提示 URL；`curl -s http://127.0.0.1:18765/health` 期望输出 **`ok`**（与 `hjs-egress-ip-cli/README.md` 第 31 行一致）；页面 **「一键检测」** 与 CLI 摘要的互补关系（同一工具链、不同入口）。
    8. **检查清单（VER-01+02）：** Markdown **表格**，列至少包含：`步骤` | `Verge 核对` | `CLI / 审计 UI` | `通过判据`。行数 ≥ 4。
    9. **术语：** 与 `local-split-vps.md` 中 Extend、全局扩展、`PROXY`/主组名替换说法一致。
  </action>
  <verify>test -f docs/clash-verge/verification-playbook.md && rg -n "检查清单|hjs-egress-ip serve|18765|Connections|Rule" docs/clash-verge/verification-playbook.md</verify>
  <acceptance_criteria>
    - 文件 `docs/clash-verge/verification-playbook.md` 存在
    - 该文件含二级标题字面串 `检查清单` 或正文含 `检查清单` 作为表格标题邻近字串（`rg -n "检查清单" docs/clash-verge/verification-playbook.md` 有匹配）
    - 该文件同时含字面串 `hjs-egress-ip serve` 与 `127.0.0.1:18765` 与 `ok`
    - 该文件含字面串 `scenario`（与 `--simple-tsv` 说明相关）
    - 该文件含 `Connections` 或「连接」之一
    - `rg -n "https://[a-zA-Z0-9.-]+/.+/(clash|subscribe)" docs/clash-verge/verification-playbook.md` 无匹配
  </acceptance_criteria>
</task>

<task type="execute">
  <name>更新 docs/clash-verge/README.md 入口</name>
  <files>docs/clash-verge/README.md</files>
  <read_first>docs/clash-verge/README.md</read_first>
  <read_first>docs/clash-verge/verification-playbook.md</read_first>
  <action>
    在 `docs/clash-verge/README.md` 的列表中增加一条 Markdown 链接，指向 **`verification-playbook.md`**，文案标明为「可重复验证 / 与 hjs-egress-ip 对照」（用语可微调，路径必须正确）。
  </action>
  <verify>rg -n "verification-playbook.md" docs/clash-verge/README.md</verify>
  <acceptance_criteria>
    - `docs/clash-verge/README.md` 含相对链接 `verification-playbook.md`（`rg -n "verification-playbook.md" docs/clash-verge/README.md` 有匹配）
  </acceptance_criteria>
</task>

<task type="execute">
  <name>local-split-vps.md 交叉引用 playbook</name>
  <files>docs/clash-verge/local-split-vps.md</files>
  <read_first>docs/clash-verge/local-split-vps.md</read_first>
  <action>
    在 **§5.4 分流（Clash）和 `hjs-egress-ip` 有啥关系？** 段末或段内，增加一句明确链到 **`verification-playbook.md`**（Markdown 链接），说明固定步骤已收敛到该文。
  </action>
  <verify>rg -n "verification-playbook.md" docs/clash-verge/local-split-vps.md</verify>
  <acceptance_criteria>
    - `local-split-vps.md` 含 `verification-playbook.md` 链接（`rg` 有匹配）
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `rg -n "https://[a-zA-Z0-9.-]+/.+/(clash|subscribe)" docs/clash-verge/verification-playbook.md docs/clash-verge/README.md docs/clash-verge/local-split-vps.md || true` 对 playbook 无命中
- [ ] `test -f docs/clash-verge/verification-playbook.md`
- [ ] `rg -n "verification-playbook.md" docs/clash-verge/README.md docs/clash-verge/local-split-vps.md`
</verification>

<success_criteria>
- VER-01：步骤级说明 + Verge 可观察指标 + 可重复顺序
- VER-02：CLI 与 `serve` 审计 UI 时机、解读与检查清单表格对齐
- 入口与主说明交叉引用完整，无公开敏感 URL
</success_criteria>
