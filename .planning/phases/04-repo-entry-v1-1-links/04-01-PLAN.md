---
phase: 04-repo-entry-v1-1-links
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - VER-03
must_haves:
  - "根 README 含指向 docs/clash-verge/README.md、local-split-vps.md、verification-playbook.md、hjs-egress-ip-cli/README.md、verge/README.md 的相对 Markdown 链接"
  - "根 README 明确：推荐 pipx 安装 CLI、详情见 hjs-egress-ip-cli/README；根不抄写可复制安装命令（D-01）"
  - "根 README 含「仅克隆」与「不等于已安装」或等价纠偏句（D-02）"
  - "根 README 将 Verge 订阅 + 全局扩展粘贴为主路径；verge/scripts 为辅助、非使用分流规则的必需前置步骤（D-05/D-06）"
  - "公开树 README 无真实订阅 URL（rg 口径与 Phase 3 一致）"
  - "导航表中链到的仓库内路径在磁盘上均存在"
---

# Plan 04-01：根 README 入口链与 VER-03 叙事（repo-entry-v1-1-links）

<objective>
更新仓库根 `README.md`：在保留现有「仓库结构」表与「快速分流」句的前提下，新增 **v1.1 文档与工具链**小节（导航表 + 安装路径澄清 + 主路径/辅助脚本纠偏），满足 **VER-03** 与 **04-CONTEXT** 中 **D-01～D-06**；交付前以 `test -f` 与 `rg` 验收链接与敏感信息。
</objective>

<threat_model>
**ASVS L1（文档）：** 防止在根 `README.md` 写入真实订阅 URL、令牌或节点密钥。**缓解：** 仅使用相对链到既有文档；不粘贴 `https://.../clash/...` 类面板链接；示例保持占位表述。**残余：** 作者编辑失误 — 由 `acceptance_criteria` 中 `rg` 兜底。
</threat_model>

<tasks>

<task type="execute">
  <name>根 README：新增 v1.1 导航与安装/主路径叙事</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <read_first>.planning/phases/04-repo-entry-v1-1-links/04-CONTEXT.md</read_first>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <read_first>verge/README.md</read_first>
  <action>
    在 `README.md` 中，于现有「仓库结构」表**之后**、**「快速分流」段落之前或之后**（择一保持阅读流畅；若放在之后，须在「快速分流」段末加一句指向下文「v1.1 文档与工具链」）插入二级标题 **`## v1.1 文档与工具链`**，并包含：

    1. **三级标题 `### 文档导航`**：Markdown **表格**，两列 **`文档`**、**`说明`**，至少 **5 行**（不含表头），每行第一列须为指向下列文件的 **相对链接**（路径字面一致）：
       - `docs/clash-verge/README.md` — 分流说明目录入口
       - `docs/clash-verge/local-split-vps.md` — 自用分流与工具链说明
       - `docs/clash-verge/verification-playbook.md` — 可重复验证与审计联动
       - `hjs-egress-ip-cli/README.md` — CLI 与审计 UI；**pipx/安装命令仅在此文**
       - `verge/README.md` — Clash Verge 扩展粘贴工作流；可选脚本定位

    2. **三级标题 `### 安装与使用路径`**：连续短文（2～4 句），必须满足：
       - 出现字面 **`pipx`**，并说明 **推荐**通过 **`pipx` 安装** `hjs-egress-ip`，**具体命令仅见** `[hjs-egress-ip-cli/README.md](hjs-egress-ip-cli/README.md)`，**根 README 不抄写** `pip install` / `pipx install` 等可复制命令行。
       - 出现 **「仅克隆」** 与 **「不等于已安装」**（或 **「并不意味着已安装」** 二选一）的纠偏表述，指向子目录 README 完成安装。
       - 一句提及 **Python 3.9+** 与工具链细节见 **`hjs-egress-ip-cli/`** 与 **`verge/README.md`**，且**不得**暗示「分流进 Verge 须先跑 Python 合并脚本作为默认步骤」。

    3. **三级标题 `### 分流与验证主路径（与 Verge 内置能力一致）`**：连续短文，必须满足：
       - 明确 **HTTPS 订阅在 Verge 内维护** + **`verge/extend/global-split.yaml` 粘贴进全局扩展** 为使用本仓库分流规则的**主路径**（可与现有「快速分流」句互相引用，避免重复时可写「同上」但须保留指向 `verge/README.md` 的链接至少一处）。
       - 明确 **`verge/scripts/`** 下脚本为 **辅助**（调试、占位符、可选离线合并等），**不是**读者使用本仓库分流规则的 **必需前置步骤**。
       - 包含指向 Clash Verge 官方 Extend 文档的 Markdown 链接，**URL 字面**为 `https://www.clashverge.dev/guide/extend.html`。
       - 用一句话指向 **`verification-playbook.md`** 作为验证步骤收敛文档。

    不删除现有「v1.0 归档」句；不修改 `hjs-egress-ip-cli` 与 `verge/` 下除 README 外的文件。
  </action>
  <verify>rg -n "v1\.1 文档与工具链|文档导航|安装与使用路径" README.md</verify>
  <acceptance_criteria>
    - `README.md` 含二级标题行 `## v1.1 文档与工具链`（`rg -n '^## v1\.1 文档与工具链' README.md` 有匹配）
    - `README.md` 含三级标题 `### 文档导航` 与 `### 安装与使用路径` 与 `### 分流与验证主路径`（`rg -n "### 文档导航|### 安装与使用路径|### 分流与验证主路径" README.md` 至少 3 处匹配）
    - `README.md` 同时含相对链 `docs/clash-verge/README.md`、`docs/clash-verge/local-split-vps.md`、`docs/clash-verge/verification-playbook.md`、`hjs-egress-ip-cli/README.md`、`verge/README.md`（`rg -n "docs/clash-verge/README\.md|local-split-vps\.md|verification-playbook\.md|hjs-egress-ip-cli/README\.md|verge/README\.md" README.md` 均有匹配）
    - `README.md` 含字面 `pipx` 与 `仅克隆` 与（`不等于已安装` **或** `并不意味着已安装`）之一（`rg -n "pipx" README.md`；`rg -n "仅克隆" README.md`；`rg -n "不等于已安装|并不意味着已安装" README.md` 有匹配）
    - `README.md` **不**含形如 shell 命令行的安装调用：行首可选空白后紧跟 `pipx install` 或 `pip install`（`rg -n "^[[:space:]]*(pipx|pip)[[:space:]]+install" README.md` **无**匹配）
    - `README.md` 含 `https://www.clashverge.dev/guide/extend.html` 字面 URL（`rg -n "https://www\.clashverge\.dev/guide/extend\.html" README.md` 有匹配）
    - `README.md` 含「辅助」且含「必需」或「前置」之一用于否定脚本为必需（`rg -n "辅助" README.md`；`rg -n "必需|前置" README.md` 有匹配）
  </acceptance_criteria>
</task>

<task type="execute">
  <name>链接目标存在性与敏感信息抽检</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
    在仓库根目录执行下列存在性检查（路径相对仓库根）：
    `test -f docs/clash-verge/README.md`
    `test -f docs/clash-verge/local-split-vps.md`
    `test -f docs/clash-verge/verification-playbook.md`
    `test -f hjs-egress-ip-cli/README.md`
    `test -f verge/README.md`
    `test -f verge/extend/global-split.yaml`
    全部须退出码 0。另对 `README.md` 运行敏感订阅 URL 模式检查：`rg -n "https://[a-zA-Z0-9.-]+/.+/(clash|subscribe)" README.md` 须**无输出**（无匹配则命令可非零，以无输出为准）。
  </action>
  <verify>test -f docs/clash-verge/README.md && test -f README.md</verify>
  <acceptance_criteria>
    - `test -f docs/clash-verge/README.md` 退出码 0
    - `test -f docs/clash-verge/local-split-vps.md` 退出码 0
    - `test -f docs/clash-verge/verification-playbook.md` 退出码 0
    - `test -f hjs-egress-ip-cli/README.md` 退出码 0
    - `test -f verge/README.md` 退出码 0
    - `test -f verge/extend/global-split.yaml` 退出码 0
    - `rg -n "https://[a-zA-Z0-9.-]+/.+/(clash|subscribe)" README.md || true` 在标准输出上**无**行输出
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `rg -n '^## v1\.1 文档与工具链' README.md`
- [ ] `test -f docs/clash-verge/README.md && test -f docs/clash-verge/verification-playbook.md && test -f hjs-egress-ip-cli/README.md && test -f verge/README.md`
- [ ] `rg -n "^[[:space:]]*(pipx|pip)[[:space:]]+install" README.md` 无匹配
- [ ] `rg -n "https://[a-zA-Z0-9.-]+/.+/(clash|subscribe)" README.md || true` 无 stdout 输出
</verification>

<success_criteria>
- VER-03：根 README 链向分流说明目录、CLI 的 pipx 叙事（链出细节）、Verge/扩展工作流；无新建私有仓、不改 Verge 软件本体
- D-01～D-06：安装命令仅在子 README；克隆纠偏；主路径为 Verge 内置扩展与订阅；脚本为辅助非必需前置
- 导航链相对路径全部有效、无断链
</success_criteria>
