---
phase: 02-clash-verge-split-docs
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - verge/scripts/merge_subscription_with_extend.py
  - verge/scripts/fetch_subscription_nodes.py
  - verge/tests/test_merge_subscription_with_extend.py
  - verge/tests/test_fetch_subscription_nodes.py
  - verge/README.md
  - docs/clash-verge/local-split-vps.md
autonomous: true
requirements:
  - CV-03
  - CV-04
must_haves:
  - "D-13: 脚本路径对齐 Verge Extend 主路径之外的「可选单文件合并」与 fetch 对照；不替代 HTTPS 订阅"
  - "D-14: Python 3.9+；macOS/Windows 文档与测试可在两平台执行（路径与启动器）"
  - "D-15: 合并实现处理策略组名对齐；订阅 rules 与扩展 rules 覆盖/追加策略在 CLI 与文档中显式化"
  - 提供 `verge/scripts/merge_subscription_with_extend.py`：输入订阅 YAML + 扩展 YAML，输出 `verge/out/merged.profile.yaml`（默认用扩展 `rules` 覆盖订阅 `rules`，可用 `--keep-subscription-rules` 追加，行为写进 `--help`）
  - `fetch_subscription_nodes.py` 增加 `--dry-run` 或等价仅校验 URL 不写出（可选）；至少保留现有行为并通过测试 mock
  - `verge/tests/` 下 pytest 用例覆盖合并与拉取核心分支
---

# Plan 02-03：订阅合并工具与自动化验证（CV-03、CV-04）

<objective>
实现 **可选**「订阅 + 扩展 → 单文件」合并 CLI，满足 CV-03 中与 REQUIREMENTS 一致的 **脚本拉取对照** 能力；用 **pytest** 固化行为，满足 VALIDATION.md 采样策略；文档与 `verge/README.md` 引用该工具。
</objective>

<threat_model>
**T-2-01 订阅 URL 泄露：** URL 通过 CLI 参数或环境变量传入，**不得**写入日志或仓库。**缓解：** 打印输出仅含本地输出路径；示例使用 `SUBSCRIPTION_URL`。**SSRF：** 拉取脚本仅用于维护者本机；文档声明勿对不可信 URL 使用；测试使用 `unittest.mock` 模拟 `urlopen`。
</threat_model>

<tasks>

<task type="execute">
  <name>新增 merge_subscription_with_extend.py</name>
  <files>verge/scripts/merge_subscription_with_extend.py</files>
  <read_first>verge/extend/global-split.yaml</read_first>
  <read_first>.planning/phases/02-clash-verge-split-docs/02-PATTERNS.md</read_first>
  <read_first>.planning/phases/02-clash-verge-split-docs/02-CONTEXT.md</read_first>
  <action>
    使用 **PyYAML**（在 `verge/scripts` 旁增加 `requirements-dev.txt` 含 `pyyaml`，或文档要求 `pip install pyyaml`）。实现 CLI：
    - 参数：`--subscription`（文件路径）、`--extend`（默认 `verge/extend/global-split.yaml`）、`-o`（默认 `verge/out/merged.profile.yaml`）、`--keep-subscription-rules`（布尔：为真时将订阅 `rules` 前置追加到扩展 `rules` 前，并在注释说明与默认覆盖的差异）。
    - 合并语义：**保留**订阅的 `proxies` 与 `proxy-providers`（若有）；**保留**订阅的 `proxy-groups`；将扩展中的 `dns`、`geodata-mode`、`geo-auto-update`、`geo-update-interval`、`geox-url`、`sniffer` 与扩展的 `rules` 按 PATTERNS.md 写入结果；`mode`、`log-level`、`ipv6` 以扩展为准若存在。
    - **组名校验：** 若扩展 `rules` 引用的策略组名在合并后的 `proxy-groups` 中不存在，**退出码 3** 并 stderr 列出缺失组名（可测）。
  </action>
  <verify>python3 verge/scripts/merge_subscription_with_extend.py --help</verify>
  <acceptance_criteria>
    - `merge_subscription_with_extend.py --help` 退出码 0 且帮助文本含 `--keep-subscription-rules`
    - 新建文件路径为 `verge/scripts/merge_subscription_with_extend.py`（若执行器调整命名，须同步更新 Plan 01 文档中的脚本名）
  </acceptance_criteria>
</task>

<task type="execute">
  <name>pytest：合并与拉取</name>
  <files>verge/tests/test_merge_subscription_with_extend.py, verge/tests/test_fetch_subscription_nodes.py</files>
  <read_first>verge/scripts/fetch_subscription_nodes.py</read_first>
  <read_first>verge/scripts/merge_subscription_with_extend.py</read_first>
  <action>
    1. 新建 `verge/tests/`，fixture：最小订阅 YAML（含 `proxies`、`proxy-groups`、两条简单 `rules`）+ 最小扩展片段（含 `rules` 引用订阅里存在的组名）。
    2. 断言：默认模式下输出 YAML 的 `rules` 与扩展一致（条数或首末行）；`--keep-subscription-rules` 模式下订阅规则出现在扩展规则之前。
    3. 对 `fetch_subscription_nodes.py`：mock `urllib.request.urlopen` 返回字节流，断言写出文件内容与退出码 0。
  </action>
  <verify>python3 -m pytest verge/tests -q</verify>
  <acceptance_criteria>
    - `pytest verge/tests -q` 退出码 0
    - 至少 2 个测试函数收集（合并 + 拉取各至少 1）
  </acceptance_criteria>
</task>

<task type="execute">
  <name>文档与 README 引用合并命令</name>
  <files>verge/README.md, docs/clash-verge/local-split-vps.md</files>
  <read_first>verge/README.md</read_first>
  <read_first>docs/clash-verge/local-split-vps.md</read_first>
  <action>
    在 `verge/README.md` 增加 **「可选合并」** 小节：示例命令  
    `python3 verge/scripts/merge_subscription_with_extend.py --subscription verge/out/fetched.yaml -o verge/out/merged.profile.yaml`  
    并提醒先 `fetch_subscription_nodes.py` 或自行准备订阅文件。更新 `local-split-vps.md` 中 Plan 01 预留的脚本名与实际一致。
  </action>
  <verify>rg -n "merge_subscription_with_extend" verge/README.md docs/clash-verge/local-split-vps.md</verify>
  <acceptance_criteria>
    - `verge/README.md` 与 `local-split-vps.md` 均包含子串 `merge_subscription_with_extend`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `python3 -m pytest verge/tests -q`
- [ ] `python3 verge/scripts/merge_subscription_with_extend.py --help`
</verification>

<success_criteria>
- CV-03：Extend 主路径仍在文档首位；合并脚本为可选增强，行为与 `--help` 一致
- CV-04：脚本与测试在 macOS/Windows 文档中已覆盖启动方式（继承 Plan 01）
</success_criteria>
