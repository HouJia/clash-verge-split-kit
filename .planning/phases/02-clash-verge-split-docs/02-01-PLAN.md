---
phase: 02-clash-verge-split-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/clash-verge/local-split-vps.md
  - docs/clash-verge/README.md
  - verge/README.md
autonomous: true
requirements:
  - CV-01
  - CV-04
must_haves:
  - "D-01: 自用说明主路径固定在 docs/clash-verge/（README 入口 + local-split-vps.md）；根 README 链入 defer Phase 4"
  - "D-02: 规划工件留在 .planning/，用户可读正文在 docs/clash-verge/ 与 verge/"
  - "D-03: 中文叙述；术语对齐 Clash Verge 界面；内核行为处标注 mihomo/Meta"
  - "D-04: 写明 Rule 模式、主策略组与订阅组名对齐、GEOSITE cn + geolocation-!cn + GEOIP CN 与仅 MATCH 的差异"
  - "D-07: Extend 全局→订阅合并顺序对照 + CV-03 产物导入说明；非对外点击教程"
  - "D-08: 无仓库内真实订阅链接/商业评价；多订阅用占位名"
  - "D-09: 文档侧核对清单：示例无真实 URL/密钥；提醒泄露后轮换"
  - "D-10: 对照说明「大表 profile」与「短 GEOSITE/GEOIP 扩展」两条维护路径及只合节点不含 dns/rules 的风险"
  - "D-12: 禁止写入本机 profile 真实路径或含密钥内容；仅私有对照"
  - docs/clash-verge/ 下阐明 Extend 主路径与可选单文件合并产物（对齐 D-13 文档面、D-14 跨平台说明）
  - 文档含 macOS 与 Windows 并列命令（python3 / py -3）
---

# Plan 02-01：自用说明与跨平台运行入口（CV-01、CV-04）

<objective>
收敛 **中文自用说明**：与 ROADMAP 成功标准第 1、4、5 条及 REQUIREMENTS CV-01/CV-04 对齐；明确 **Verge Extend 主路径** 与 **可选合并脚本产物** 的边界，避免读者误以为必须运行合并才能日常使用。
</objective>

<threat_model>
**ASVS L1（文档面）：** 防止在公开树中意外粘贴真实订阅与密钥（STRIDE：信息泄露）。**缓解：** 文档只使用占位符与 `verge/templates/*.example.yaml` 路径；合并产物目录固定 `verge/out/`（gitignored）。**残余风险：** 作者手动复制粘贴失误 — 由 D-09 核对清单与 PR 自检兜底。
</threat_model>

<tasks>

<task type="execute">
  <name>补齐 docs/clash-verge 入口与结构</name>
  <files>docs/clash-verge/README.md, docs/clash-verge/local-split-vps.md</files>
  <read_first>docs/clash-verge/local-split-vps.md</read_first>
  <read_first>.planning/phases/02-clash-verge-split-docs/02-CONTEXT.md</read_first>
  <action>
    1. 若尚不存在 `docs/clash-verge/README.md`：新建为 **短入口页**（中文），用列表链到 `local-split-vps.md`、`../../verge/README.md`，并加一句「根 README 链入在 Phase 4（VER-03）」。
    2. 更新 `local-split-vps.md`：在「主路径」节后增加小节 **「可选：单文件合并产物」**，说明用途（备份/对照/离线导入）、输出路径约定 `verge/out/merged.profile.yaml`、与 Extend 主路径二选一关系；引用即将在 Plan 03 实现的脚本名（`verge/scripts/merge_subscription_with_extend.py` 或 PLAN 最终命名）。
    3. 写清 **客户端版本免责声明**（成功标准第 5 条）：以本机 Clash Verge 为准。
  </action>
  <verify>test -f docs/clash-verge/README.md && rg -n "merged.profile.yaml|Extend|Phase 4" docs/clash-verge/README.md docs/clash-verge/local-split-vps.md</verify>
  <acceptance_criteria>
    - `docs/clash-verge/README.md` 存在且包含指向 `local-split-vps.md` 的 Markdown 链接
    - `local-split-vps.md` 含字面字符串 `verge/out/` 与 `Extend`
    - `rg -n "https://[a-zA-Z0-9.-]+/.+/(clash|subscribe)" docs/clash-verge` 无匹配（避免示例真实订阅 URL）
  </acceptance_criteria>
</task>

<task type="execute">
  <name>verge/README.md 跨平台命令块</name>
  <files>verge/README.md</files>
  <read_first>verge/README.md</read_first>
  <action>
    在 `verge/README.md` 增加 **「macOS / Windows」** 小节：并列给出  
    - macOS：`python3 verge/scripts/render_global_split.py` 与 `SUBSCRIPTION_URL=... python3 verge/scripts/fetch_subscription_nodes.py`  
    - Windows：`py -3 verge\scripts\render_global_split.py`（路径写法用说明性文字，避免假设盘符）  
    并注明 Python **3.9+**。
  </action>
  <verify>rg -n "py -3|python3|Windows|macOS" verge/README.md</verify>
  <acceptance_criteria>
    - `verge/README.md` 同时包含字面串 `py -3` 与 `python3`
    - `verge/README.md` 包含 `3.9` 或 `3.9+`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `rg -n "https://[a-zA-Z0-9.-]+/.+/(clash|subscribe)" docs/clash-verge || true` 无命中
- [ ] `test -f docs/clash-verge/README.md`
</verification>

<success_criteria>
- CV-01：自用说明覆盖分流概念、Extend 主路径、可选合并产物与导入关系
- CV-04：文档层完成跨平台命令对照
- 无真实敏感 URL/密钥写入公开文档
</success_criteria>
