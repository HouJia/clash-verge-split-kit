---
phase: 01-hjs-egress-ip-cli-tool
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - hjs-egress-ip-cli/src/hjs_egress_ip/web/**
  - hjs-egress-ip-cli/src/hjs_egress_ip/web/static/index.html
  - hjs-egress-ip-cli/pyproject.toml
  - hjs-egress-ip-cli/README.md
autonomous: true
requirements: []
must_haves:
  - `hjs-egress-ip serve` 在本机可启动，`GET /health` 返回 `ok`
  - 静态 `index.html` 随包分发（importlib.resources 或等价）
  - `python -m build` 可产出 wheel（或文档记录已知限制）
---

# Plan 01-03：Web 子命令与打包验证

**目标：** 将 `serve-audit-ui.py` 迁入包内；提供 `hjs-egress-ip serve`；验证构建产物。

<context>

- 参考：`~/.cursor/skills/hjs-egress-ip-audit/scripts/serve-audit-ui.py`
- 参考：`~/.cursor/skills/hjs-egress-ip-audit/web/index.html`
- 依赖：`01-02` 中 CLI 已能调用探测逻辑

</context>

<tasks>

<task type="auto">
  <name>包内 web 模块与静态资源</name>
  <files>hjs-egress-ip-cli/src/hjs_egress_ip/web/server.py, hjs-egress-ip-cli/src/hjs_egress_ip/web/static/index.html</files>
  <read_first>~/.cursor/skills/hjs-egress-ip-audit/scripts/serve-audit-ui.py</read_first>
  <action>迁移 HTTP 处理逻辑到 `hjs_egress_ip.web.server`（或等价路径）；`index.html` 放入包内并通过 `importlib.resources.files(...).joinpath("index.html")` 读取。API 行为与现版一致：`POST /api/run` 的 `mode` simple|full、调用 CLI 或 bash 子进程。</action>
  <verify>test -f hjs-egress-ip-cli/src/hjs_egress_ip/web/static/index.html</verify>
  <acceptance_criteria>
    - 包内存在 `web/static/index.html`
  </acceptance_criteria>
  <done>不依赖仓库外相对路径即可加载页面</done>
</task>

<task type="auto">
  <name>注册 serve 子命令</name>
  <files>hjs-egress-ip-cli/src/hjs_egress_ip/cli.py, hjs-egress-ip-cli/pyproject.toml</files>
  <read_first>hjs-egress-ip-cli/src/hjs_egress_ip/web/server.py</read_first>
  <action>使用 `argparse` 子命令：`hjs-egress-ip serve [--port 18765] [--no-open]`，默认绑定 `127.0.0.1`。若 `[project.optional-dependencies]` 无额外库则省略 `web` extra；否则锁定版本。</action>
  <verify>cd hjs-egress-ip-cli && pip install -e . && (timeout 2 hjs-egress-ip serve --no-open 2>&1 | head -1 || true)</verify>
  <acceptance_criteria>
    - `hjs-egress-ip serve --help` 退出码 0
  </acceptance_criteria>
  <done>子命令可解析</done>
</task>

<task type="auto">
  <name>构建与 README 发布占位</name>
  <files>hjs-egress-ip-cli/README.md, hjs-egress-ip-cli/pyproject.toml</files>
  <read_first>hjs-egress-ip-cli/README.md</read_first>
  <action>本地执行 `python -m build`（需 `build` 前端工具或文档注明 `pip install build`）。README 增加「构建 wheel」「PyPI / Homebrew 占位流程」小节。</action>
  <verify>cd hjs-egress-ip-cli && python -m build 2>&1 | tail -3</verify>
  <acceptance_criteria>
    - `dist/` 下生成 `.whl` 或在 README 明确记录当前环境无法构建时的替代验证步骤
  </acceptance_criteria>
  <done>发布路径有文字说明</done>
</task>

</tasks>

<verification>
- [ ] `hjs-egress-ip serve --no-open &` 后 `curl -s http://127.0.0.1:18765/health` 为 `ok`（端口以默认或 README 为准）
- [ ] `python -m build` 成功或文档已说明例外
</verification>

<success_criteria>
- Web 与 CLI 使用同一探测后端，抽样对比 TSV 与迁移前一致
</success_criteria>

<output>
完成后创建 `01-03-SUMMARY.md`。
</output>
