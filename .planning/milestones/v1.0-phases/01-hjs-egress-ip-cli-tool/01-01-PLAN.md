---
phase: 01-hjs-egress-ip-cli-tool
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - hjs-egress-ip-cli/README.md
  - hjs-egress-ip-cli/LICENSE
  - hjs-egress-ip-cli/.gitignore
  - hjs-egress-ip-cli/pyproject.toml
autonomous: true
requirements: []
must_haves:
  - 存在可编辑安装的 Python 包目录 `hjs-egress-ip-cli/`，含 README、LICENSE、.gitignore、pyproject.toml
  - `pyproject.toml` 中声明 `project.scripts` 入口 `hjs-egress-ip`（可先指向占位 `main`）
---

# Plan 01-01：仓库骨架与打包元数据

**目标：** 建立独立工具目录与 `pyproject.toml`，为后续挂载真实 CLI 做准备。

<context>

- 参考现状：`~/.cursor/skills/hjs-egress-ip-audit/scripts/egress-ip-audit.sh`
- 父路径：本仓库 `airport/` 下新建 `hjs-egress-ip-cli/`（若用户另有 Git 根目录，可等价调整并更新本文件路径描述）

</context>

<tasks>

<task type="auto">
  <name>创建 hjs-egress-ip-cli 目录与基础文件</name>
  <files>hjs-egress-ip-cli/README.md, hjs-egress-ip-cli/LICENSE, hjs-egress-ip-cli/.gitignore</files>
  <read_first>.planning/milestones/v1.0-phases/01-hjs-egress-ip-cli-tool/CONTEXT.md</read_first>
  <action>在 `airport/hjs-egress-ip-cli/` 创建目录。README 用中文写清：工具目的、与全局技能 `hjs-egress-ip-audit` 的关系、安装方式占位（`pip install -e .`）。LICENSE 选 MIT（或团队既定许可证）。`.gitignore` 覆盖 Python 常见产物（`__pycache__/`、`.venv/`、`dist/`、`*.egg-info/` 等）。</action>
  <verify>test -f hjs-egress-ip-cli/README.md && test -f hjs-egress-ip-cli/LICENSE && test -f hjs-egress-ip-cli/.gitignore</verify>
  <acceptance_criteria>
    - `README.md` 含关键词 `hjs-egress-ip` 与 `pip install -e`
  </acceptance_criteria>
  <done>三文件存在且 README 非空</done>
</task>

<task type="auto">
  <name>添加 pyproject.toml（hatchling 或 setuptools）</name>
  <files>hjs-egress-ip-cli/pyproject.toml, hjs-egress-ip-cli/src/hjs_egress_ip/__init__.py</files>
  <read_first>hjs-egress-ip-cli/README.md</read_first>
  <action>使用 `pyproject.toml` 定义包名 `hjs-egress-ip`，构建后端任选 `hatchling` 或 `setuptools`；包布局采用 `src/hjs_egress_ip/`。`project.scripts` 注册 `hjs-egress-ip = "hjs_egress_ip.cli:main"`。先实现 `cli.py` 中 `main()` 打印 `--help` 或占位文案，确保可导入无循环依赖。</action>
  <verify>cd hjs-egress-ip-cli && pip install -e . && hjs-egress-ip --help</verify>
  <acceptance_criteria>
    - `pyproject.toml` 存在 `[project.scripts]` 且键为 `hjs-egress-ip`
    - `hjs-egress-ip --help` 退出码 0
  </acceptance_criteria>
  <done>可编辑安装成功，入口命令可运行</done>
</task>

</tasks>

<verification>
- [ ] `cd hjs-egress-ip-cli && pip install -e . && hjs-egress-ip --help`
- [ ] `cd hjs-egress-ip-cli && python -c "import hjs_egress_ip; import hjs_egress_ip.cli"`
</verification>

<success_criteria>
- 所有 `<task>` 完成且 verify 通过
- 无未捕获的导入或打包错误
</success_criteria>

<output>
完成后创建 `01-01-SUMMARY.md`（由执行工作流写入）。
</output>
