---
phase: 01-hjs-egress-ip-cli-tool
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - hjs-egress-ip-cli/src/hjs_egress_ip/cli.py
  - hjs-egress-ip-cli/src/hjs_egress_ip/data/probes.default.yaml
  - hjs-egress-ip-cli/tests/**
  - hjs-egress-ip-cli/README.md
autonomous: true
requirements: []
must_haves:
  - 默认无参行为与当前技能脚本「一键摘要」等价（或子进程包装 bash 且输出一致）
  - 支持 `--simple-tsv`、`--full`、`--ipv6-skip`、`--interface`
  - `pytest` 对解析/分组纯函数有测试；README 说明与技能的关系及 pipx 推荐
---

# Plan 01-02：CLI 行为、配置外置与测试

**目标：** 将探测逻辑接入 CLI；探测点数据迁入 YAML；补充单元测试与文档。

<context>

- 源脚本：`~/.cursor/skills/hjs-egress-ip-audit/scripts/egress-ip-audit.sh` 中 `PROBES` 与参数语义
- 依赖计划：`01-01` 已完成可安装包骨架

</context>

<tasks>

<task type="auto">
  <name>迁移 PROBES 至 probes.default.yaml</name>
  <files>hjs-egress-ip-cli/src/hjs_egress_ip/data/probes.default.yaml</files>
  <read_first>~/.cursor/skills/hjs-egress-ip-audit/scripts/egress-ip-audit.sh</read_first>
  <action>在包内 `src/hjs_egress_ip/data/probes.default.yaml`（可用 `importlib.resources` 读取）列出与当前 bash `PROBES` 等价的探测项：`category`、`name`、`url`、`parser`。实现加载函数：文件缺失或 YAML 语法错误时 `sys.exit` 前打印明确中文/英文错误。</action>
  <verify>cd hjs-egress-ip-cli && python -c "from hjs_egress_ip.probes import load_default; load_default()"</verify>
  <acceptance_criteria>
    - YAML 至少包含 4 个 category（domestic、intl_plain、cdn_trace、meta）各至少 1 条
  </acceptance_criteria>
  <done>加载函数可导入且返回非空探测列表</done>
</task>

<task type="auto">
  <name>实现 CLI：默认一键、--simple-tsv、--full 与进阶参数</name>
  <files>hjs-egress-ip-cli/src/hjs_egress_ip/cli.py</files>
  <read_first>hjs-egress-ip-cli/src/hjs_egress_ip/data/probes.default.yaml</read_first>
  <action>首版推荐：`subprocess` 调用已安装技能路径下的 `egress-ip-audit.sh`（通过环境变量 `HJS_EGRESS_AUDIT_SCRIPT` 可覆盖），将参数原样透传；或翻译为 Python 调 `curl`（次版）。`main()` 无子命令时等价于「无参一键摘要」。实现 `--simple-tsv`、`--full`、`--ipv6-skip`、`--interface IFACE`，与 bash 脚本行为对齐。</action>
  <verify>cd hjs-egress-ip-cli && pip install -e . && hjs-egress-ip 2>&1 | head -5</verify>
  <acceptance_criteria>
    - `hjs-egress-ip --help` 列出上述参数
  </acceptance_criteria>
  <done>默认与 `--simple-tsv` / `--full` 三种路径均可执行</done>
</task>

<task type="auto">
  <name>抽取解析/TSV/分组逻辑并 pytest</name>
  <files>hjs-egress-ip-cli/src/hjs_egress_ip/formatters.py, hjs-egress-ip-cli/tests/test_formatters.py</files>
  <read_first>hjs-egress-ip-cli/src/hjs_egress_ip/cli.py</read_first>
  <action>将「单行 TSV 字段解析」「按 category 分组」「httpbin JSON / cip HTML 摘要抽取」中与 UI 无关的逻辑拆到 `formatters.py`（或等价模块）。`tests/` 使用 fixture 覆盖：空 body、非法 JSON、HTML 无 IP 行。</action>
  <verify>cd hjs-egress-ip-cli && pytest -q</verify>
  <acceptance_criteria>
    - `pytest -q` 退出码 0
  </acceptance_criteria>
  <done>至少 3 个用例通过</done>
</task>

<task type="auto">
  <name>更新 README：技能关系与 pipx</name>
  <files>hjs-egress-ip-cli/README.md</files>
  <read_first>hjs-egress-ip-cli/README.md</read_first>
  <action>增加章节：与 `~/.cursor/skills/hjs-egress-ip-audit` 的并列关系；推荐最终用户通过 `pipx install` / `pipx run` 使用 CLI；说明可选环境变量 `HJS_EGRESS_AUDIT_SCRIPT`。</action>
  <verify>rg -n "pipx|技能|hjs-egress-ip-audit" hjs-egress-ip-cli/README.md</verify>
  <acceptance_criteria>
    - README 同时出现 `pipx` 与 `hjs-egress-ip-audit` 或等价描述
  </acceptance_criteria>
  <done>文档与实现一致</done>
</task>

</tasks>

<verification>
- [ ] `cd hjs-egress-ip-cli && pytest -q`
- [ ] `hjs-egress-ip --simple-tsv | head -3` 含表头 `scenario`
</verification>

<success_criteria>
- 配置加载失败路径有清晰报错
- 测试全绿
</success_criteria>

<output>
完成后创建 `01-02-SUMMARY.md`。
</output>
