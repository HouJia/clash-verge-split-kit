---
phase: 01-hjs-egress-ip-cli-tool
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - ~/.cursor/skills/hjs-egress-ip-audit/SKILL.md
  - vpn-reality-guides/README.md
  - README.md
autonomous: true
requirements: []
must_haves:
  - 全局技能 `SKILL.md` 以「已安装 CLI」为第一推荐调用方式，脚本路径为 fallback
  - airport 仓库内至少一处文档指向 `hjs-egress-ip-cli/README.md`（若用户选择不对外宣传则可仅改 SKILL 并在 SUMMARY 说明「跳过 D2」）
---

# Plan 01-04：技能与仓库文档衔接

**目标：** 更新全局技能说明；在 airport 内增加指向工具 README 的入口（按产品意愿可选）。

<context>

- 技能路径：`~/.cursor/skills/hjs-egress-ip-audit/SKILL.md`
- 工具路径：`airport/hjs-egress-ip-cli/README.md`（随 01-01 创建）

</context>

<tasks>

<task type="auto">
  <name>更新全局技能 SKILL.md</name>
  <files>~/.cursor/skills/hjs-egress-ip-audit/SKILL.md</files>
  <read_first>~/.cursor/skills/hjs-egress-ip-audit/SKILL.md</read_first>
  <action>调整「执行流程」：优先写 `pipx run hjs-egress-ip` / `hjs-egress-ip`（安装后）；保留 `scripts/egress-ip-audit.sh` 作为未安装 CLI 时的 fallback。注明环境变量 `HJS_EGRESS_AUDIT_SCRIPT`（若 CLI 实现采用子进程包装）。</action>
  <verify>rg -n "pipx|hjs-egress-ip[^-]" ~/.cursor/skills/hjs-egress-ip-audit/SKILL.md</verify>
  <acceptance_criteria>
    - `SKILL.md` 正文优先出现独立 CLI 调用说明
  </acceptance_criteria>
  <done>技能与工具分工清晰</done>
</task>

<task type="auto">
  <name>airport 仓库内交叉引用（可选 D2）</name>
  <files>vpn-reality-guides/README.md, README.md</files>
  <read_first>airport/.planning/ROADMAP.md</read_first>
  <action>若存在 `vpn-reality-guides/README.md` 或根 `README.md`：增加一句链向 `hjs-egress-ip-cli/README.md`（相对仓库根）。若均不存在：在本计划 SUMMARY 中记录「跳过 D2」及原因。</action>
  <verify>test -f README.md || test -f vpn-reality-guides/README.md || echo "no-readme-skip-ok"</verify>
  <acceptance_criteria>
    - 已添加交叉引用，或 SUMMARY 明确跳过
  </acceptance_criteria>
  <done>可发现性满足团队最低要求</done>
</task>

</tasks>

<verification>
- [ ] `rg "hjs-egress-ip-cli" . -g "*.md" | head -5` 有命中，或 SUMMARY 说明跳过
</verification>

<success_criteria>
- 新用户从技能文档能走到「如何安装 CLI」
</success_criteria>

<output>
完成后创建 `01-04-SUMMARY.md`。
</output>
