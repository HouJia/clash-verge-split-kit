# 需求：airport（里程碑 v1.1）

**定义：** 2026-05-03  
**仓库可见性：** 公开；订阅 URL、令牌、节点密钥**不得**写入 Git，仅本机/私有配置。  
**节点策略：** **VPS 自架订阅为主**，第三方机场订阅为辅（合并与默认策略组以 PLAN 为准）。  
**核心价值（沿用）：** 一键按站点类型梳理出口 IP 探测结果；`--full` 保留完整矩阵能力。

## v1.1 需求（本里程碑）

### Clash Verge 分流（CV）

- [ ] **CV-01**：在仓库内维护**自用**中文说明：Clash Verge / mihomo 侧「分流」核心概念（规则、策略组、模式），以及本仓库**自动化合并**产物如何导入客户端；供作者本人复现与迭代，**不是**对外教程。
- [ ] **CV-02**：提供**已脱敏**的规则与配置骨架（`verge/extend/global-split.yaml`、`verge/templates/*.example.yaml`），展示至少一种可验证分流模式，**不包含**真实订阅 URL 或密钥。
- [ ] **CV-03（自动化主路径）**：以 Clash Verge **内置** [Extend](https://www.clashverge.dev/guide/extend.html) 能力实现：**HTTPS 订阅**（自动更新节点）+ **全局扩展配置**（粘贴仓库 `verge/extend/global-split.yaml`，覆盖/合并 `dns`/`geodata`/`rules` 等）；**只保留订阅里的节点与策略组，规则用仓库版本**。可选：脚本将订阅拉至 `verge/out/` 供本机核对，**不**替代 Verge 订阅机制。
- [ ] **CV-04（跨机复用）**：上述自动化流程在 **macOS 与 Windows** 上均可运行（优先与现有 **Python 3.9+ / `pipx`** 工具链一致；Windows 须有文档化安装与运行方式），便于在多台电脑复用**同一套规则仓库 + 同一类订阅入口**（各机使用本机私有 URL 配置，**不把真实链接提交进 Git**）。

### 验证与审计联动（VER）

- [x] **VER-01**：定义可重复的**验证流程**（步骤级）：如何在切换规则/节点后确认某类流量走 DIRECT 或指定策略组（含 Clash Verge 内可观察指标与系统侧检查建议）。— *Phase 3 已验证*
- [x] **VER-02**：将上述验证流程与现有 **`hjs-egress-ip` / 审计 UI** 对齐：明确何时运行 CLI、如何解读输出与分流预期是否一致，并形成简短检查清单。— *Phase 3 已验证*
- [x] **VER-03（Phase 4）**：在仓库根 `README` 里加**链接**：分流说明目录、`hjs-egress-ip` 的 `pipx` 安装方式、若有则含订阅合并工具用法；**不**新建「私有仓库」，也不改 Clash Verge 软件本身。— *2026-05-04 已满足*

## 后续版本（暂不排期）

### 自动化

- **AUTO-01**：可选：egress 探测结果与上次的差异摘要（与 CV-03 分流合并工具可分开迭代）。

## 明确不做

| 内容 | 原因 |
|------|------|
| 在公开 Git 中保存真实订阅 URL、Token、节点密钥 | 仅本机/私有配置或环境变量；仓库内只用占位符 |
| Fork 或打包 Clash Verge | 交付合并配置与工具链，不改客户端本体 |
| 为分流合并单独承诺 GUI 安装包 | 优先 CLI/脚本 + 文档 |

## 追溯

| 需求 | 阶段 | 状态 |
|------|------|------|
| CV-01 | 2 | 待定 |
| CV-02 | 2 | 待定 |
| CV-03 | 2（或与 2/5 拆分，以 PLAN 为准） | 待定 |
| CV-04 | 2（或与 2/5 拆分，以 PLAN 为准） | 待定 |
| VER-01 | 3 | 已满足 |
| VER-02 | 3 | 已满足 |
| VER-03 | 4 | 已满足 |

**覆盖：** v1.1 需求 7 条；CV-* 主要映射 Phase 2（自动化落点以 `/gsd-plan-phase` 为准）；VER-* 映射 Phase 3–4。

---
*需求定义：2026-05-03 · 2026-05-04 补充 CV-03/CV-04（订阅 URL 自动化合并 + 跨平台）· 里程碑 v1.1*
