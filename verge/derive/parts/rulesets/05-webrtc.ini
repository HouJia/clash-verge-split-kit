; =============================================================================
; 系统 · 📡 WebRTC — STUN / TURN UDP（按端口，覆盖各厂商，不限 Google）
; =============================================================================
; 策略组定义见 parts/20-routing.ini（待下一迭代接入 custom_proxy_group）
; 规则须排在 compose 顺序最前（00-private 之后），避免被 GeoSite 抢先
; 默认出口 REJECT；Verge / 旁路由 OpenClash 面板可改选地区组做试验
; =============================================================================

ruleset=系统 · 📡 WebRTC,[]AND,((NETWORK,udp),(DST-PORT,3478))
ruleset=系统 · 📡 WebRTC,[]AND,((NETWORK,udp),(DST-PORT,5349))
ruleset=系统 · 📡 WebRTC,[]AND,((NETWORK,udp),(DST-PORT,19302))
